apply plugin: 'java'
apply plugin: 'war'

ext.sourceCompatibility=JavaVersion.VERSION_1_8

repositories { jcenter() }

configurations {
	providedCompile
}

dependencies {

}

task wrapper(type: Wrapper) {
    gradleVersion = '2.14'
}
